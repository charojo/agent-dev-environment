[project]
name = "agent-dev-environment"
version = "0.1.0"
description = "Tooling and workflows for Agentic Development"
requires-python = ">=3.10"
dependencies = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
]

[tool.uv]
dev-dependencies = []
